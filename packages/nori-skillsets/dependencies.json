{
  "$comment": "Dependencies required by the seaweed CLI at runtime. Versions are pulled from the main package.json during build.",
  "dependencies": [
    "ajv",
    "ajv-formats",
    "commander",
    "firebase",
    "glob",
    "semver",
    "tar",
    "winston"
  ]
}
