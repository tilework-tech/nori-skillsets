#!/usr/bin/env bash
# tmux-isolated: Wrapper enforcing isolation for direct tmux commands
#
# Usage: tmux-isolated <tmux-args>
#
# Automatically adds: -f /dev/null -L nori-agent-sock

set -euo pipefail

exec tmux -f /dev/null -L "nori-agent-sock" "$@"
