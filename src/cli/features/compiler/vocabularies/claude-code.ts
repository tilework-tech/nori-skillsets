/**
 * Claude Code vocabulary mapping (identity transform)
 * Since the canonical format IS Claude dialect, no tool names need swapping.
 */
export const claudeCodeVocabulary: Record<string, string> = {};
